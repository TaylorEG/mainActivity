<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.evanlee.g2lbuddyandroid">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">


        <activity android:name=".InputReading"/>

        <activity
            android:name=".journalAverages"
            android:label="Journal Averages"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".journalInput"
            android:label="journalInput"
            android:theme="@style/AppTheme.NoActionBar"/>

        <activity android:name=".printActivity" />

        <activity android:name=".DailyData" />

    </application>

</manifest>